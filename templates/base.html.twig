<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>F1 DATA</title>
		<link rel="icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
		<style>
			body {
				background-color: #212529;
				color: #f8f9fa;
			}
			.navbar {
				background-color: #343a40;
			}
			.navbar-nav .nav-link {
				color: #adb5bd;
			}
			.navbar-nav .nav-link:hover,
			.navbar-nav .nav-link.active {
				color: #ffffff;
			}
		</style>
	</head>
	<body>
		<nav class="navbar navbar-expand-lg navbar-dark">
			<div class="container-fluid">
				<a class="navbar-brand d-flex align-items-center" href="{{ path('home') }}">
					<img src="{{ asset('logo.png') }}" alt="Logo" width="30" height="30" class="d-inline-block align-text-top me-2">
					F1 DATA
				</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link" href="{{ path('team') }}">Escuderías</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('driver') }}">Pilotos</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('circuit') }}">Circuitos</a>
						</li>
					</ul>
					{% if app.user %}
						<a href="{{ path('admin') }}" class="btn btn-outline-light ms-auto">DASHBOARD</a>
					{% else %}
						<!-- Button added for /login, pushed to the right -->
						<a href="{{ path('app_login') }}" class="btn btn-outline-light ms-auto">ACCESO</a>
					{% endif %}

				</div>
			</div>
		</nav>

		<div class="container mt-4">
			{% if app.request.attributes.get('_route') == 'home' %}
				<h1>Bienvenido a la App de Fórmula 1</h1>
				<p>Explora información sobre las escuderías, pilotos y circuitos de Fórmula 1.</p>
			{% endif %}
		</div>


		{% block body %}{% endblock %}

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
	</body>
</html>
